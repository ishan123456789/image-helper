import { ImageHelperDirective } from './image-helper.directive';

describe('ImageHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageHelperDirective();
    expect(directive).toBeTruthy();
  });
});
